let start = 'Debit Card';
let strr = start.toUpperCase();
// 1.toUppercase,lowercase
console.log(strr);
let sortedStrr = strr.split('').sort().join('');
// 2.can't sort a string because sorction so splitt and join
console.log(sortedStrr); 
// map or array declaration let map =new map;
// map has two function get and set .get gives value taking key as arg set puts value  to ket taking key and val as arg .  map also has has() to check whether a key is present or not
